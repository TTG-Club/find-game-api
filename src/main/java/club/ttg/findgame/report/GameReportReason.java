package club.ttg.findgame.report;

/** Причина, по которой пользователь сообщает о нарушении в объявлении. */
public enum GameReportReason {
    ADVERTISING,
    SPAM,
    INAPPROPRIATE_IMAGE,
    OTHER
}
